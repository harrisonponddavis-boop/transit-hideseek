{
  "name": "transit-hideseek",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c yellow,cyan \"GAME_PORT=3001 npm --prefix server start\" \"npm --prefix client run dev\"",
    "test": "npm --prefix server test",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
